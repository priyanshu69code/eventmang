# Environment Variables for Event Management System
# Copy this file to .env and update values as needed

# Django Settings
SECRET_KEY=django-insecure-w=^e@m2*uq9a!^-d6xhjc^7&gx#fkwoe-5+!00=a21$3h5g9e4
DEBUG=True
ALLOWED_HOSTS=localhost,127.0.0.1

# Database Settings (SQLite is default, no configuration needed)
# For PostgreSQL:
# DATABASE_URL=postgresql://user:password@localhost:5432/eventmanagement

# Email Settings (for production)
# EMAIL_HOST=smtp.gmail.com
# EMAIL_PORT=587
# EMAIL_HOST_USER=your-email@gmail.com
# EMAIL_HOST_PASSWORD=your-app-password
# EMAIL_USE_TLS=True

# JWT Settings (optional, defaults are set in settings.py)
# JWT_ACCESS_TOKEN_LIFETIME=60
# JWT_REFRESH_TOKEN_LIFETIME=10080

# CORS Settings (for frontend integration)
# CORS_ALLOWED_ORIGINS=http://localhost:3000,http://127.0.0.1:3000

# Media and Static Files (for production)
# STATIC_ROOT=/path/to/static/files
# MEDIA_ROOT=/path/to/media/files
